type Control = {
  id: number;
  tipo: string;
  valor: string;
};

function CheckboxControls({ controls }: { controls: Control[] }) {
  return <div>Check Ctrls</div>;
}

export default CheckboxControls;
