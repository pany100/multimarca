import React from "react";
import { Typography } from "@mui/material";

const DashboardPage = () => {
  return (
    <Typography variant="body1">
      Este es el contenido principal del dashboard.
    </Typography>
  );
};

export default DashboardPage;
